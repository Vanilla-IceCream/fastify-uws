[package]
name = "warp"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
tokio = { version = "1.40.0", features = ["full"] }
warp = "0.3.7"
serde = { version = "1.0", features = ["derive"] }
