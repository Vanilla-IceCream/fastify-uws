[package]
name = "salvo"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
tokio = { version = "1.38.0", features = ["macros"] }
salvo = "0.68.4"
serde = { version = "1.0", features = ["derive"] }
