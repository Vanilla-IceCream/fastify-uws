[package]
name = "salvo"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
tokio = { version = "1.36.0", features = ["macros"] }
salvo = "0.66.2"
serde = { version = "1.0", features = ["derive"] }
